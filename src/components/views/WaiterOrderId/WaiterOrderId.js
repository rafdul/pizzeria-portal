import React from 'react';
import styles from './WaiterOrderId.module.scss';

const WaiterOrderId = () => (

  <div className={styles.component}>
    <h2>All orders view for waiter</h2>
  </div>

);

export default WaiterOrderId;
